# Lista global para almacenar los diccionarios de personas
personas = []

def agregar_persona(nombre:str, edad:int, ciudad:str):
    persona = {
        'nombre': nombre,
        'edad': edad,
        'ciudad': ciudad
    }
    personas.append(persona)

def main()
    name = input("Ingrese su nombre")
    age = int(input("Ingrese su edad")
    city = input("Ingrese la ciudad")
    agregar_personas(name, age, city) #agrega un nuevo diccionario a la lista


    for usuario in personas: #recorre toda la lista
        for usuario["edad"] > 18: #verifica a los usuarios que sean mayores de 18
              for usuario["ciudad"] == medellin 
              print(usuario["nombre"] #imprim el nombre de usuario 

    print(personas)
if __name:: == __ "main":
    main()
